- name: Install web stack packages
  apt:
    name:
      - nginx
      - php-fpm
      - certbot
      - python3-certbot-nginx
      - redis-server
    state: present
    update_cache: yes

- name: Ensure nginx is enabled
  service:
    name: nginx
    state: started
    enabled: yes
